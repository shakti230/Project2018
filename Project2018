#Mary McDonagh, 20-03-2018
# Analysis 1
#url = "https://archive.ics.uci.edu/ml/machine-learning-databases/iris/iris.data"
# ref: http://www.astro.up.pt/~sousasag/Python_For_Astronomers/PythonScientific-simple.pdf

print ("Petal Length", "Petal Width", "Sepal Length", "Sepal Width")
  
with open("data/iris.csv") as x:
     for line in x:
        print ("{:>12}{:>12}{:>13}{:>12}".format(line.split(',')[1],line.split(',')[2],line.split(','[3]))


#Analysis 2
# labelnames translates the species names into integers
labelnames = {"Iris-setosa\n":0, 
              "Iris-versicolor\n":1, 
              "Iris-virginica\n":2}
data, labels = [], []      # Empty lists for the vectors, labels.
f = open("data/iris.csv", "r") # Open the data file.
for line in f:             # Load in each line from the file. 
    vals = line.split(',') # Split the line into individual values
    if len(vals) == 5:     # Check that there are five columns
        data.append(vals[:-1])              # Add data vector
        labels.append(labelnames[vals[4]])  # Add numerical label
f.close()                  # Close the file


#Analysis 3
# labelnames translates the species names into integers
labelnames = {"Iris-setosa\n":0,
             "Iris-versicolor\n":1,
             "Iris-virginica\n":2}
data, labels = [], []      # Empty lists for the vectors, labels.
f = open("data/iris.csv", "r") # Open the data file.
for line in f:             # Load in each line from the file.
   vals = line.split(',') # Split the line into individual values
   if len(vals) == 5:     # Check that there are five columns
       data.append(vals[:-1])              # Add data vector
       labels.append(labelnames[vals[4]])  # Add numerical label
f.close()                  # Close the file


#Analysis 4
#Ref: https://www.datacamp.com/community/tutorials/exploratory-data-analysis-python
# Import the `pandas` library as `pd`
import pandas as pd

# Load in the data with `read_csv()`
digits = pd.read_csv("http://archive.ics.uci.edu/ml/machine-learning-databases/optdigits/optdigits.tra",
                     header=None)

# Print out `digits`
print(digits)

#Analysis 5
#use describe function to output data description
# Import the `pandas` library as `pd`
import pandas as pd

# Load in the data with `read_csv()`
digits = data/iris.csv
data/iris.csv.describe()

#Analysis 6
#Calculate the mean
# Import the `pandas` library as `pd`
import pandas as pd

#Reading IRIS Dataset in Pandas Dataframe
dataset = pd.read_csv("http://archive.ics.uci.edu/ml/machine-learning-databases/optdigits/optdigits.tra",
                     header=None)

dataset = pd.read_csv(url)
print(dataset.head(20))
